<aura:component>
    //add
  //  <aura:attribute name="cartList" type="Store_Product__c[]"/>
   
   
   
    


   //register the component to handle the event
    //need to reference the evt file that danny has written to be able to register the component
   <aura:handler event="c.addToCart" action="{!c.handleAddToCart}"/>

   

   <div>
   <h1>Shopping Cart</h1>
   <p>{!v.productName}{!v.productCost}\n
   </p>
   <hr/>
 
</div>

</aura:component>	
